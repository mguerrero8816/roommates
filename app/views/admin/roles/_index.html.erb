<table class="table">
  <thead>
    <tr><th><%= link_to 'New Role' %></th></tr>
    <tr>
      <th>Name</th>
      <th>Options</th>
    </tr>
  </thead>
  <tbody>
    <% @roles.each do |role| %>
      <tr>
        <td><%= role.name %></td>
        <td><%= link_to 'Edit' %> | <%= link_to 'Delete' %></td>
      </tr>
    <% end %>
  </tbody>
</table>
